services:
  random-string-gen:
    build: .
    image: random-string-gen:latest
    container_name: random-string-gen
    ports:
      - "3000:3000"          # 左边是宿主机端口，可按需修改
    environment:
      - PORT=3000
    restart: unless-stopped   # 崩溃/重启后自动拉起
